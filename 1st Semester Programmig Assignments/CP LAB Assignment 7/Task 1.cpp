/*#include<iostream>
using namespace std;
int power_f(int base, int power) {
	if (power == 0) return 1;
	else
	return base*power_f(base, power - 1);
}
int main()
{
	int base, power, res;
	cout << "Enter the Base Number: ";
	cin >> base;
	cout << "Enter the Exponent: ";
	cin >> power;
	res = power_f(base, power);
	cout << base << " is raised to the power of " << power << " is " << res;
	return 0;
}*/