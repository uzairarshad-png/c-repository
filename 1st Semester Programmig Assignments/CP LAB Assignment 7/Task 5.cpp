/*#include <iostream>
using namespace std;

// Function to find maximum of two integers
int maxint(int a, int b) {
    if (a > b)return a;
    else return b;
}

// Function to find maximum of three floating-point numbers
float maxfloat(float x, float y, float z) {
    float maxVal = x; // assume first is max initially
    if (y > maxVal) maxVal = y;
    if (z > maxVal) maxVal = z;
    return maxVal;
}

int main() {
    int int1, int2;
    float num1, num2, num3;

    // Taking input for integer comparison
    cout << "Enter two integer readings: ";
    cin >> int1 >> int2;
    cout << "The maximum of the two integers is: " << maxint(int1, int2) << endl;

    // Taking input for floating-point comparison
    cout << "\nEnter three sensor readings (floating-point numbers): ";
    cin >> num1 >> num2 >> num3;
    cout << "The highest recorded sensor value is: " << maxfloat(num1, num2, num3) << endl;
    return 0;
}
*/